import styled from '@emotion/styled';

const NavList = styled.ul`
  display: flex;
  gap: 30px;
  align-items: center;
`;

const HeaderWrapper = styled.div`
  height: 115px;
  display: flex;
  align-items: center;
`;

export default { NavList, HeaderWrapper };
